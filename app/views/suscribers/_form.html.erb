<%= form_with(model: suscriber, local: true) do |form| %>
  <% if suscriber.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(suscriber.errors.count, "error") %> prohibited this suscriber from being saved:</h2>

      <ul>
      <% suscriber.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :suscriber_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
