
<%= form_with(model: property_type, local: true) do |form| %>
  <% if property_type.errors.any? %>
    <div class="error_explanation">
      <h3><%= pluralize(property_type.errors.count, "error") %> prohibited this property_type from being saved:</h3>

      <ul>
      <% property_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <!-- general form elements -->
  <div class="box box-primary">
    
    <!-- /.box-header -->
    <!-- form start -->
    
      <div class="box-body">
        <div class="form-group">

          <%= form.label "Nom" %>
          <%= form.text_field :name, id: :property_type_name, class: "form-control", placeholder: "Nom" %>
        </div>

     <div class="form-group">
          <%= form.label :description %>
          <%= form.text_area :description, id: :property_type_description, class: "form-control", placeholder: "Description" %>
        </div>

      <div class="form-group">
          <%= form.submit "Enregistrer", class: "btn bg-purple pull-right" %>
        </div>
        </div>
        </div>
      <% end %>
