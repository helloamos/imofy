<%= form_with(model: contract_type, local: true) do |form| %>
  <% if contract_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contract_type.errors.count, "error") %> prohibited this contract_type from being saved:</h2>

      <ul>
      <% contract_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <!-- general form elements -->
  <div class="box box-primary">
    
    <!-- /.box-header -->
    <!-- form start -->
    
      <div class="box-body">
        <div class="form-group">

        <%= form.label "Nom" %>
        <%= form.text_field :name, id: :contract_type_name, class: "form-control", placeholder: "Nom" %>
      </div>

     <div class="form-group">
      <%= form.label :description %>
      <%= form.text_area :description, id: :contract_type_description , class: "form-control", placeholder: "Description" %>
    </div>

   <div class="form-group">
          <%= form.submit "Enregistrer", class: "btn bg-purple pull-right" %>
        </div>
        </div>
        </div>
      <% end %>